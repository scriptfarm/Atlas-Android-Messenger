<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.ui.Light">
        <!-- Apply default styles -->
        <item name="ConversationsRecyclerView">@style/ConversationsRecyclerView</item>
        <item name="MessagesRecyclerView">@style/MessagesRecyclerView</item>
        <item name="MessageComposer">@style/MessageComposer</item>
        <item name="AddressBar">@style/AddressBar</item>
        <item name="TypingIndicator">@style/TypingIndicator</item>
    </style>

</resources>
